[project]
name = "ale-scripted-gui"
version = "0.1.0"
description = "Scripted no-LLM GUI agent for the demo hello tasks."
requires-python = ">=3.12,<3.14"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
only-include = []
sources = {}
bypass-selection = true
